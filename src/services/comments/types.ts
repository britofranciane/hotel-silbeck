export interface Comment {
  commenter_name: string;
  rating?: number;
  comment_date?: string;
  comment: string;
  image_url?: string;
  id: number;
  title?: string;
  name: string;
  description?: string;
  date: string;
}
